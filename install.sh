#!/usr/bin/env bash
# Very little additional config is needed after cloning, but this will symlink the configs read outside this folder to their proper directories
ln -s ./.zsh ../.zsh
ln -s ./.zshrc ../.zshrc
ln -s ./.p10k.zsh ../.p10k.zsh
